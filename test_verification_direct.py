#!/usr/bin/env python3\n\"\"\"\nDirect verification test script.\n\nThis script tests the Veritas Logos verification system directly\nwithout going through the API endpoints, bypassing authentication\nand billing complications.\n\"\"\"\n\nimport sys\nimport os\nimport asyncio\nfrom datetime import datetime\nfrom pathlib import Path\n\n# Add src to path\nsys.path.insert(0, str(Path(__file__).parent / \"src\"))\n\nprint(\"Starting direct verification system test...\")\nprint(\"Testing imports...\")\n\ntry:\n    from verification.workers.verification_worker import execute_verification_chain_task\n    print(\"✅ Imported verification_worker\")\nexcept ImportError as e:\n    print(f\"❌ Failed to import verification_worker: {e}\")\n    \ntry:\n    from verification.config.chain_loader import ChainConfigLoader\n    print(\"✅ Imported ChainConfigLoader\")\nexcept ImportError as e:\n    print(f\"❌ Failed to import ChainConfigLoader: {e}\")\n    \ntry:\n    from models.verification import VerificationTask, VerificationStatus, Priority\n    print(\"✅ Imported verification models\")\nexcept ImportError as e:\n    print(f\"❌ Failed to import verification models: {e}\")\n    \ntry:\n    from document_ingestion.document_service import DocumentService\n    print(\"✅ Imported DocumentService\")\nexcept ImportError as e:\n    print(f\"❌ Failed to import DocumentService: {e}\")\n\nprint(\"\\n🔍 Testing Veritas Logos Verification System\")\nprint(\"=\" * 50)\n\n# Test document content\ntest_content = \"\"\"\n# Climate Change Analysis\n\n## Introduction\nClimate change is one of the most pressing challenges of our time. According to the Intergovernmental Panel on Climate Change (IPCC), global temperatures have risen by approximately 1.1°C since pre-industrial times.\n\n## Key Claims\n1. **Temperature Rise**: NASA's Goddard Institute for Space Studies reports that 2023 was the warmest year on record globally.\n2. **Sea Level Rise**: Sea levels have risen by approximately 8-9 inches since 1880.\n3. **Economic Impact**: The transition to renewable energy could create millions of jobs globally.\n\n## Sources\nThis document references IPCC reports, NASA data, and NOAA measurements.\n\"\"\"\n\nprint(f\"📄 Test Document Length: {len(test_content)} characters\")\nprint(\"\\nTest completed - basic imports successful!\") 